# Adish
#In this project I wrote a program that using a frozen graph(a graph that is already trained and you cant train it no more)
#My main purpose was to take a video - detecate the objects in it - replace it with a video where all the objects are markd
#my algorithm takes the video - split it to frames - detec the objects - mark on every fram - rebuilding the frames to video
